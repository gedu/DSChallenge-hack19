class ChallengeItem {
  String title;
  String imageUrl;
  String desc;
  String tags;
  List<String> githubLinks;
  int score;

  ChallengeItem(this.title, this.imageUrl, this.desc, this.tags,
      this.githubLinks, this.score);
}
